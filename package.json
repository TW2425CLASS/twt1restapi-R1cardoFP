{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "API simulada com JSON Server para alunos e cursos",
  "main": "index.js",
  "scripts": {
    "start:db": "json-server --watch ./mock-data/bd.json --port 3002",
    "start:api": "node backend/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongodb": "^6.16.0"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
